{
  "name": "freewind_software_microfrontend",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "./apps/host",
    "./apps/portfolio-react"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack workspaces --mode production",
    "start": "webpack  --parallel serve --mode development"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2"
  },
  "dependencies": {
    "mvp.css": "^1.17.1"
  }
}